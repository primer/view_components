# frozen_string_literal: true

require "primer/<%= status_path %><%= underscore_name %>"

class Primer::<%= status_module %><%= class_name %>Stories < ViewComponent::Storybook::Stories
  layout "storybook_preview"

  story(:<%= underscore_name %>) do
    controls do
      classes "custom-class"
    end

    content do
      "Update your stories!"
    end
  end
end
