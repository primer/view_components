# frozen_string_literal: true

class Primer::<%= class_name %>Stories < ViewComponent::Storybook::Stories
  layout "storybook_preview"

  story(:<%= underscore_name %>) do
    controls do
      classes "custom-class"
    end

    content do
      "Update your stories!"
    end
  end
end
