# frozen_string_literal: true

require "linter_test_case"

class ArgumentMappers<%= component_short_name %>Test < LinterTestCase
  def test_add_tests
    raise StandardError, "Add tests for your argument mapper"
  end
end
