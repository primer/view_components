name: Lock Release Pull Request

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Lock or unlock the release?'
        required: true
        type: choice
        options:
          - lock
          - unlock

jobs:
  lock:
    runs-on: ubuntu-latest
    steps:
      - name: Get App Token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ vars.PRIMER_APP_ID_SHARED }}
          owner: primer
          repositories: view_components
          private-key: ${{ secrets.PRIMER_APP_PRIVATE_KEY_SHARED }}
      - run: |
          echo "Locking release: ${{ inputs.action }}"
          curl -L \
            -X PUT \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ steps.app-token.outputs.token }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/primer/view_components/rulesets/${{ vars.RELEASE_CONDUCTOR_RULE_ID }} \
            -d '{"enforcement":"active"}'
  # gh api \
  #   --method PUT \
  #   -H "Accept: application/vnd.github+json" \
  #   -H "X-GitHub-Api-Version: 2022-11-28" \
  #   /repos/primer/view_components/rulesets/${{ vars.RELEASE_CONDUCTOR_RULE_ID }} \
  #   -f "enforcement=enabled"
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
