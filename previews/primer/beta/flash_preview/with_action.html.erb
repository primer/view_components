<%= render(Primer::Beta::Flash.new(full: full, dismissible: dismissible, icon: icon, scheme: scheme)) do |component| %>
  <% component.with_action(size: :medium) { "Take action" } %>
  <%= content %>
<% end %>
