<%= render(Primer::Alpha::Dialog.new(title: title, subtitle: subtitle, auto_open: auto_open)) do |d| %>
  <% d.with_show_button { @button_text } %>
  <% d.with_body { "Content" } %>
  <% d.with_footer do %>
    render(Primer::ButtonComponent.new(scheme: :primary)) { "Submit" }
  <% end %>
<% end %>
