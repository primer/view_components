#!/bin/bash

# Gems
bundle config set --local path 'vendor/bundle'
bundle install
npm install

# Set up demo
pushd demo
bundle config set --local path 'vendor/bundle'
bundle install
npm install
popd

# Initial build of docs content
bundle exec rake docs:build
