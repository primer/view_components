<%= render Primer::BaseComponent.new(**@system_arguments) do %>
  <% if content.present? %>
    <%= content %>
  <% else %>
    <%= DateTime.parse(@datetime).strftime("%B %-d, %Y %H:%M %Z") %>
  <% end %>
<% end %>
