---
title: Check Box Group
id: check_box_group_input
---

Check box groups consist of one or more related check boxes.

## Usage

```ruby
class ExampleForm < ApplicationForm
  form do |example_form|
    example_form.check_box_group(attributes) do |group|
      group.check_box(check_box_attributes)
    end
  end
end
```


## Common arguments

| Name | Type | Description |
| :- | :- | :- |
| `hidden` | Boolean | When set to `true`, visually hides the group. |
| `caption` | String | A string describing the field and what sorts of input it expects. Displayed below the group. |
| `label_arguments` | Hash | Attributes that will be passed to Rails' `builder.label` method. These can be HTML attributes or any of the other label options Rails supports. They will appear as HTML attributes on the `<label>` tag. |


## Methods

### `#check_box`

Adds a check box to the group.



| Name | Type | Description |
| :- | :- | :- |
| `system_arguments` | Hash | The arguments accepted by [CheckBox](<%= page_path(:check_box_input) %>). |
| `block` | Proc | The block accepted by [CheckBox](<%= page_path(:check_box_input) %>). |


## Examples

<%= embed Primer::Forms::FormsPreview, :check_box_group_form %>
