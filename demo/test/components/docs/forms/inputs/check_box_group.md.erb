---
title: Check Box Group
id: check_box_group_input
---

Check boxes are true/false inputs rendered as `<input type="checkbox">` in HTML.

## Usage

```ruby
class ExampleForm < ApplicationForm
  form do |example_form|
    example_form.check_box_group(attributes) do |group|
      group.check_box(check_box_attributes)
    end
  end
end
```


## Common arguments

| Name | Type | Default | Description |
| :- | :- | :- | :- |
| `hidden` | Boolean | N/A | When set to `true`, visually hides the group. |
| `caption` | String | N/A | A string describing the field and what sorts of input it expects. Displayed below the group. |
| `label_arguments` | Hash | N/A | Attributes that will be passed to Rails' `builder.label` method. These can be HTML attributes or any of the other label options Rails supports. They will appear as HTML attributes on the `<label>` tag. |


## Methods

### `#check_box`

Adds a check box to the group.



| Name | Type | Default | Description |
| :- | :- | :- | :- |
| `system_arguments` | Hash | N/A | The arguments accepted by [CheckBox](/components/alpha/checkbox). |
| `block` | Proc | N/A | The block accepted by [CheckBox](/components/alpha/checkbox). |

